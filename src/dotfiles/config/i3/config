# i3 config file (v4o

# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout somewhen, delete
# this file and re-run i3-config-wizard(1).
#

# Please see http://i3wm.org/docs/userguide.html for a complete reference!

# Alt
set $mod Mod1

# Workspace names
# set $ws1 " 1  Web / Docs"
set $ws1 " 1  Browser "
set $wsMoneyBrowser " 1  Browser "

set $ws2 " 2  Browser "
set $wsBrowser2 " 2  Browser "

set $ws3 " 3  Browser "
set $wsBrowser3 " 3  Browser "

set $ws4 " 4  "
set $wsRando " 4  "

set $ws5 " 5   "
set $wsCode " 5   "

set $ws6 " 6   "
set $wsCode2 " 6   "

set $ws7 " 7  VM"
set $wsVM " 7  VM"

set $ws8 " 8  Mail"
set $wsMail " 8  Mail"

set $ws9 " 9  󱂅 "
set $wsServices " 9  󱂅 "

set $ws10 " 10  "
set $wsMedia " 10  "

set $ws11 " 11  Music"
set $wsMusic " 11  Music"

set $ws12 " 12 󰍡 Messaging "
set $wsMessaging " 12 󰍡 Messaging "

# "Windows" key
#set $mod Mod4
# set $mod Super_L

### Gaps config
gaps inner 3
gaps outer 3
smart_borders on

# Only enable gaps on a workspace when there is at least one container
smart_gaps on

# Only enable outer gaps when there is exactly one container
smart_gaps inverse_outer

# new_window pixel 3
# for_window [class=".*"] normal 3
default_border normal 3
# border_radius 5
### END Gaps config

# font for window titles. ISO 10646 = Unicode
# font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
# font xft:Inconsolata Medium 11
# font xft:Oxygen-Sans 12
# font pango: MesloLGMDZ Nerd Font Mono 8
# font pango: FiraCode Nerd Font Mono 10
font xft: FiraCode Nerd Font Mono 10


# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod
floating_minimum_size 50 x 50
# floating_maximum_size 1440 x 1440

# Scratchpad settings
# send window to the scratch pad. Use what is typically my minimize shortcut
bindsym $mod+Control+i move scratchpad
# Show the scratchpad
bindsym $mod+Shift+i scratchpad show, move position center, border normal
bindsym $mod+Tab scratchpad show, move position center, border normal

# Use the xprop command to get window info
for_window [title="^.*jeff.* - Gmail - .*$"] move container to workspace $wsMail, border normal
for_window [title="^.*jeff@sabledynamics.com.*Private Email.*"] move container to workspace $wsMail, border normal
for_window [title="^.*jeffb@perceptview.com - PerceptView Mail.*"] move container to workspace $wsMail, border normal

for_window [title="^.*WhatsApp.*"] move container to workspace $wsMessaging
for_window [title="^.*Discord.*"] move container to workspace $wsMessaging
for_window [title="^.*Messages for web.*"] move container to workspace $wsMessaging
for_window [instance="slack"] move container to workspace $wsMessaging
for_window [class="Slack"] move container to workspace $wsMessaging
for_window [title="^Slack.*"] move container to workspace $wsMessaging
for_window [title="^Signal.*"] move container to workspace $wsMessaging

for_window [title="^Transmission.*"] move container to workspace $wsMedia, floating disable
for_window [title="^.*PassThePopcorn.*"] move container to workspace $wsMedia

for_window [title="^Oracle VM VirtualBox Manager.*"] move container to workspace $wsVM


# Color config
# include ~/.config/i3/theme-solarized-config
include ~/.config/i3/theme-latte-config

# for_window [class="konsole"] border pixel 4
# for_window [class="kitty"] border pixel 2
for_window [title="QjackCtl"] floating enable, move position center, border normal
for_window [title="^Audacity.*"] floating enable  move position center, border normal
for_window [title="^Zoom.*"] floating enable,  move position center, border normal

# For Neovim peek(markdown) plugin
# do not focus preview window on open
no_focus [title="^Peek preview.*$"]


# https://userbase.kde.org/Tutorials/Using_Other_Window_Managers_with_Plasma#I3_configuration
# Plasma stuff
# Kill the plasma wallpaper
# for_window [class="plasmashell" title="Desktop — Plasma"] kill; floating enable; border none
for_window [title="Desktop — Plasma"] kill; floating enable; border none
for_window [class="plasmashell" title="^Desktop @ QRect.*$"] kill; floating enable; border none
for_window [class="plasmashell" window_type="notification"] floating enable, border none, move position center
no_focus [class="plasmashell" window_type="notification"]
for_window [title="Plasma"] floating enable, border none
for_window [title="Plasma-desktop"] floating enable, border none
for_window [title="plasma-desktop"] floating enable, border none
for_window [class="win7"] floating enable, border none
for_window [class="krunner"] floating enable, border none
for_window [class="Yakuake"] floating enable, border pixel 2
for_window [title="Yakuake"] floating enable, border pixel 2
for_window [class="Kmix"] floating enable, border none,  move position center
for_window [class="kruler"] floating enable; border none,  move position center
for_window [class="klipper"] floating enable; border none,  move position center
for_window [class="Kcalc"] floating enable, border normal 3,  move position center
for_window [class="Plasmoidviewer"] floating enable, border none,  move position center
for_window [class="(?i)*nextcloud"] floating disable,  move position center
# no_focus [class="plasmashell"]
for_window [class="plasmashell" window_type="notification"] floating enable, border none
no_focus [class="plasmashell" window_type="notification"]

for_window [class="plasmawindowed"] resize set 640 680, floating enable, move position mouse


for_window [class="vlc"] resize set 1440 1440, floating enable,  move position center, border normal
for_window [class="Mplayer"] resize set 1440 1440, floating enable,  move position center, border normal
for_window [class="^.*mpv$"] resize set 1440 1440, floating enable,  move position center, border normal
for_window [class="qjackctl"] floating enable,  move position center, border normal

# for_window [class="NC"] floating enable
# for_window [class="Cadence.py"] floating enable
# for_window [class="Skype"] floating enable
for_window [title="^Android Emulator.*"] floating enable, border normal 3,  move position center


for_window [class="dde-desktop"] floating enable, border none
for_window [class="dde-osd"] floating enable, border none
for_window [class="Klipper"] floating enable, border none
# for_window [title="Plex"] floating enable, sticky enable, border normal
for_window [title="^Plex$"]  resize set 1440 1440, floating enable, sticky enable, move position center, border normal
for_window [title="^.*YouTube$"] resize set 1440 1440, floating enable, sticky enable, move position center, border normal
# for_window [title="Netflix"] floating enable, sticky enable, border normal,  move position center
for_window [title="Netflix"] resize set 1440 1440, floating enable, sticky enable, move position center, border normal
for_window [title="^Prime Video.*$"] resize set 1440 1440, floating enable, sticky enable, move position center, border normal
for_window [title="Amarok"] floating enable,  move position center, border normal
for_window [class="Deadbeef"] floating enable, move position center, border normal

# Float by type
# for_window [window_role="pop-up" title=".*(?!)YouTube$"] floating enable, border normal
for_window [window_role="bubble"] floating enable, border none
for_window [window_role="task_dialog"] floating enable, border none
for_window [window_role="Preferences"] floating enable
for_window [window_role="About"] floating enable
for_window [window_type="dialog"] floating enable
for_window [window_type="menu"] floating enable, border none
for_window [title="gkrellm"] floating enable, enable sticky, border none
for_window [class="jamesdsp"] floating enable, enable sticky, border normal,  move position center


no_focus [window_role="pop-up"]
no_focus [window_type="notification"]
# >>> Avoid tiling for non-Plasma stuff <<<
for_window [window_role="pop-up"] floating enable, border pixel 2
for_window [window_role="bubble"] floating enable, border pixel 2
for_window [window_role="task_dialog"] floating enable, border pixel 2
for_window [window_role="Preferences"] floating enable, border pixel 2
for_window [window_role="About"] floating enable, border pixel 2
for_window [window_type="dialog"] floating enable, border pixel 2
for_window [window_type="menu"] floating enable, border pixel 2
for_window [instance="__scratchpad"] floating enable, border normal 2, move position center

for_window [title=".*Audio Volume — System Settings.*"] floating disable
for_window [class="Pulseeffects"] move container to workspace $wsMusic
# for_window [instance="^play\\.google.*$"] floating disable, move container to workspace $wsMusic
for_window [title=".*Google Play Music.*"] floating disable, move container to workspace $wsMusic
for_window [title="^YouTube Music.*"] floating disable, move container to workspace $wsMusic
for_window [title=".*MellowPlayer*"] move container to workspace $wsMusic
for_window [title="^Spotify Premium$"] move container to workspace $wsMusic
for_window [title="^Spotify$"] move container to workspace $wsMusic

# for_window [floating] move position center


# switch to workspace
bindsym $mod+1 workspace $ws1
bindsym $mod+2 workspace $ws2
bindsym $mod+3 workspace $ws3
bindsym $mod+4 workspace $ws4
bindsym $mod+5 workspace $ws5
bindsym $mod+6 workspace $ws6
bindsym $mod+7 workspace $ws7
bindsym $mod+8 workspace $ws8
bindsym $mod+9 workspace $ws9
bindsym $mod+0 workspace $ws10
bindsym $mod+minus workspace $ws11
bindsym $mod+equal workspace $ws12

# move focused container to workspace
bindsym $mod+Shift+exclam move container to workspace $ws1
bindsym $mod+Shift+at move container to workspace $ws2
bindsym $mod+Shift+numbersign move container to workspace $ws3
bindsym $mod+Shift+dollar move container to workspace $ws4
bindsym $mod+Shift+percent move container to workspace $ws5
bindsym $mod+Shift+asciicircum move container to workspace $ws6
bindsym $mod+Shift+ampersand move container to workspace $ws7
bindsym $mod+Shift+asterisk move container to workspace $ws8
bindsym $mod+Shift+parenleft move container to workspace $ws9
bindsym $mod+Shift+parenright move container to workspace $ws10
bindsym $mod+Shift+underscore move container to workspace $ws11
bindsym $mod+Shift+plus move container to workspace $ws12


hide_edge_borders smart
# workspace_auto_back_and_forth yes



 #Pressing $mod and current workspace number takes you back to the previousle active workspace
# workspace_auto_back_and_forth yes

# Make the focused window very obvious
# window colors: border background text
# colorclass border background text indicator
#

# Yellow
# client.focused #b58900 #b58900 #FFFFFF
# Red
# client.focused #dc322f #dc322f #FFFFFF
# Green

default_orientation auto
popup_during_fullscreen smart

# start a terminal
bindsym $mod+Return exec konsole --hide-menubar --profile light
bindsym $mod+Shift+Return exec konsole --hide-menubar --profile dark

bindsym $mod+F12 exec konsole --hide-menubar --profile red
bindsym $mod+ctrl+t exec --no-startup-id wezterm
# bindsym $mod+ctrl+t exec --no-startup-id alacritty
#bindsym $mod+Return exec i3-sensible-terminal

# kill focused window
bindsym $mod+Control+x kill
# bindsym $mod+Mod4+x kill

# start dmenu (a program launcher)
bindsym $mod+d exec dmenu_run

# change focus
bindsym $mod+h focus left
bindsym $mod+k focus up
bindsym $mod+l focus right
bindsym $mod+j focus down

# Toggle border between default, none and 1px
bindsym $mod+t border toggle

bindsym $mod+Shift+H move left
bindsym $mod+Shift+K move up
bindsym $mod+Shift+L move right
bindsym $mod+Shift+J move down

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+Shift+V split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen
bindsym $mod+Shift+f fullscreen global

# change container layout (stacked, tabbed, default)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout default

# toggle tiling / floating
# bindsym $mod+Shift+space floating toggle
bindsym $mod+Control+F floating toggle
bindsym $mod+Control+S sticky toggle
# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent
bindsym $mod+shift+A focus child

# focus the child container
#bindcode $mod+d focus child

# reload the configuration file
bindsym $mod+Shift+C reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+R restart
# bindsym $mod+Shift+R exec ~/.i3/genconfig.sh; restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+E exit

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym l resize shrink width 1 px or 1 ppt
        bindsym k resize grow height 1 px or 1 ppt
        bindsym j resize shrink height 1 px or 1 ppt
        bindsym h resize grow width 1 px or 1 ppt

        # same bindings, but for the arrow keys
        bindsym 113 resize shrink width 1 px or 1 ppt
        bindsym 116 resize grow height 1 px or 1 ppt
        bindsym 111 resize shrink height 1 px or 1 ppt
        bindsym 114 resize grow width 1 px or 1 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}



bindsym $mod+r mode "resize"

#userid Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
# bar {
#     font xft:FiraCode Nerd Font Mono 12
# #         #font xft:DejaVu Sans 16
# #         # font xft:Oxygen-Sans Bold 10
# #         # font xft:Meslo LG S Regular for Powerline Plus Nerd File Types Plus Font Awesome Plus Pomicons 12
# #         # font pango: Meslo LG S DZ Regular for Powerline Nerd Font Plus Font Awesome Plus Octicons Mono
# #         font pango: MesloLGMDZ Nerd Font Mono 12
# #         # mode hide
# #         modifier Mod1
#     status_command i3status
#     position top
#     tray_output primary
# }

# My own hotkeys
# bindsym $mod+F1 exec google-chrome-stable --profile-directory=Default --enable-logging --v=1, exec google-chrome-stable --profile-directory='Profile 12' --enable-logging --v=1
bindsym $mod+F1 exec google-chrome-stable --profile-directory=Default, exec google-chrome-stable --profile-directory='Profile 12'
bindsym $mod+F2 exec dolphin
bindsym $mod+F3 exec ~/bin/dev-serve.sh
bindsym $mod+shift+F3 exec ~/bin/dev-serve.sh -n

# bindsym $mod+F4 workspace $wsMusic, exec google-chrome-stable --app='https://music.youtube.com/?source=pwa' --profile-directory=Default --enable-logging --v=1
bindsym $mod+F4 workspace $wsMusic, exec brave --app='https://music.youtube.com/?source=pwa'

# bindsym $mod+F5 exec google-chrome-stable --app='https://youtube.com' --profile-directory=Default
bindsym $mod+F5 exec brave --app='https://youtube.com'

# bindsym $mod+F6 exec google-chrome-stable --app='https://app.plex.tv/web/app#' --profile-directory=Default
bindsym $mod+F6 exec brave --app='https://app.plex.tv/web/app#'

# bindsym $mod+F6 exec export QT_QPA_PLATFORM=xcb && plex-desktop
# bindsym $mod+F7 exec google-chrome-stable --app='https://www.netflix.com/browse' --profile-directory=Default
bindsym $mod+F7 exec brave --app='https://www.netflix.com/browse'

# bindsym $mod+F8 exec google-chrome-stable --app='https://www.amazon.com/' --profile-directory=Default
bindsym $mod+F8 exec brave --app='https://www.amazon.com/Amazon-Video/b?node=2858778011&ref_=nav_em__aiv_0_2_3_2'

# bindsym $mod+F9 workspace $wsMessaging, exec slack, exec signal-desktop  # Started at startup
bindsym $mod+Escape exec krunner
bindsym $mod+ctrl+Escape exec rofi -show combi
# bindsym $mod+Escape exec ulauncher
bindsym $mod+ctrl+l exec ~/bin/fuzzy_screen_lock

# Move workspace left/right to different output
bindsym $mod+ctrl+greater move workspace to output right
bindsym $mod+ctrl+less move workspace to output left

# Jump to urgent window
bindsym $mod+x [urgent=latest] focus

set $mode_system System (l) lock, (e) logout, (s) suspend, (h) hibernate, (r) reboot, (Shift+s) shutdown
mode "$mode_system" {
    bindsym l exec --no-startup-id $Locker, mode "default"
    bindsym e exec --no-startup-id i3-msg exit, mode "default"
    bindsym s exec --no-startup-id $Locker && systemctl suspend, mode "default"
    bindsym h exec --no-startup-id $Locker && systemctl hibernate, mode "default"
    bindsym r exec --no-startup-id systemctl reboot, mode "default"
    bindsym Shift+s exec --no-startup-id systemctl poweroff -i, mode "default"

    # back to normal: Enter or Escape
    bindsym Return mode "default"
    bindsym Escape mode "default"
}

# Enable the menu bar for power management defined above
bindsym $mod+Pause mode "$mode_system"

# media keys
# bindcode 166 exec --no-startup-id playerctl stop
# bindcode 164 exec --no-startup-id playerctl play-pause
# bindcode 165 exec --no-startup-id playerctl previous
# bindcode 163 exec --no-startup-id playerctl next

# Media player controls
bindsym XF86AudioPlay exec playerctl play-pause
bindsym XF86AudioPause exec playerctl play-pause
bindsym XF86AudioStop exec playerctl stop
bindsym XF86AudioNext exec playerctl next
bindsym XF86AudioPrev exec playerctl previous

bindsym XF86AudioRaiseVolume exec --no-startup-id qdbus org.kde.kglobalaccel /component/kmix invokeShortcut "increase_volume"
bindsym XF86AudioLowerVolume exec --no-startup-id qdbus org.kde.kglobalaccel /component/kmix invokeShortcut "decrease_volume"
bindsym XF86AudioMute exec --no-startup-id qdbus org.kde.kglobalaccel /component/kmix invokeShortcut "mute"
bindsym XF86AudioMicMute exec --no-startup-id qdbus org.kde.kglobalaccel /component/kmix invokeShortcut "mic_mute"

exec_always ~/.fehbg
exec_always ~/bin/nasa-pod.sh &

exec --no-startup-id nm-applet

# exec_always xmodmap ~/.Xmodmap
exec_always xrdb -merge ~/.Xresources
exec yakuake
exec xflux -z 83703

exec_always --no-startup-id $HOME/.config/polybar/launch.sh

exec --no-startup-id wmctrl -c Plasma

include ~/.config/i3/`~/bin/pick_i3_config.sh`-config

exec_always --no-startup-id $HOME/bin/pick_i3_config.sh &
