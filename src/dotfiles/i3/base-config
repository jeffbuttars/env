# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout somewhen, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod1

# font for window titles. ISO 10646 = Unicode
# font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
# font xft:Droid Sans Regular 11
# font xft:Inconsolata Medium 11
# font xft:Eurostile Bold  10
# font xft:Ubuntu Bold 11
# font xft:DejaVu Sans Bold 11
# font xft:Oxygen-Sans 12
font pango: MesloLGMDZ Nerd Font Mono 10
# font xft:Meslo LG S Regular for Powerline Plus Nerd File Types Plus Font Awesome Plus Pomicons 12
# font pango:Meslo LG L DZ, FontAwesome 8
# font pango: Meslo LG S DZ Regular for Powerline Nerd Font Plus Font Awesome Plus Octicons 10

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod
floating_minimum_size 50 x 50

# Scratchpad settings
# send window to the scratch pad. Use what is typically my minimize shortcut
bindsym $mod+Control+i move scratchpad
# Show the scratchpad
bindsym $mod+Shift+i scratchpad show

# Let Yakuake float by defaul
# Use the xprop command to get window info
for_window [class="plasmashell"] floating enable, border none
for_window [class="plasmashell" title="Desktop"] floating disable, border none
for_window [class="google-chrome"] border pixel 2
for_window [class="konsole"] border pixel 2
# for_window [title="Plasma"] floating enable, border none
for_window [class="Yakuake"] floating enable
for_window [title="Yakuake"] floating enable
for_window [class="Smplayer"] floating enable
for_window [class="Kmix"] floating enable
for_window [class="Kcalc"] floating enable
for_window [class="Vlc"] floating enable
for_window [class="Mplayer"] floating enable
# Juniper VPN client, float it
for_window [class="NC"] floating enable
for_window [class="Skype"] floating enable
for_window [title="^Android Emulator.*"] floating enable

for_window [class="Plasma-desktop"] floating enable, border none
for_window [class="Plasmoidviewer"] floating enable, border none
for_window [class="krunner"] floating enable, border none
for_window [class="Klipper"] floating enable, border none
# for_window [id="qpaeq"] floating enable, border 1px
for_window [title="YouTube TV"] floating enable, sticky enable, border pixel 3
for_window [title="YouTube on TV"] floating enable, sticky enable, border pixel 3
for_window [title="Audio Volume — System Settings Module"] floating disable, border pixel 3
for_window [title="Plex"] floating enable, sticky enable, border pixel 3
for_window [title="Netflix"] floating enable, sticky enable, border pixel 3
for_window [title="Amarok"] floating enable, sticky enable, border pixel 3
for_window [class="urxvt"] floating enable, border pixel 3
for_window [class="Deadbeef"] floating enable, border pixel 2
for_window [class="^.*URxvt.*$"] floating enable, border pixel 3
for_window [class="Pulseeffects"] floating enable, border none

# Float by type
for_window [window_role="pop-up"] floating enable
for_window [window_role="task_dialog"] floating enable
for_window [window_type="dialog"] floating enable
for_window [window_type="menu"] floating enabl

no_focus [class="plasmashell"]
no_focus [window_role="pop-up"]
no_focus [window_type="notification"]

# Solaraized colors:
set $sol_base03  #002b36
set $sol_base02  #073642
set $sol_base01  #586e75
set $sol_base00  #657b83
set $sol_base0   #839496
set $sol_base1   #93a1a1
set $sol_base2   #eee8d5
set $sol_base3   #fdf6e3
set $sol_yellow  #b58900
set $sol_orange  #cb4b16
set $sol_red     #dc322f
set $sol_magenta #d33682
set $sol_violet  #6c71c4
set $sol_blue    #268bd2
set $sol_cyan    #2aa198
set $sol_green   #859900

# Workspace names
# set $ws1 " 1  "
# set $ws2 " 2  "
# set $ws3 " 3  "
# set $ws4 " 4  "
# set $ws5 " 5  "
# set $ws6 " 6  "
# set $ws7 " 7 ? "
# set $ws8 " 8 ? "
# set $ws9 " 9  "
# set $ws10 " 10  "

# Make the focused window very obvious
# window colors: border background text
# colorclass border background text indicator
#

# Yellow
# client.focused #b58900 #b58900 #FFFFFF
# Red
# client.focused #dc322f #dc322f #FFFFFF
# Green
client.focused #859900 #859900 #FFFFFF


# A client which currently has the focus.
# client.focused #002b36 $sol_base3 #002b36 $sol_red
# client.focused $sol_base03 $sol_base02 $sol_base3 $sol_red

# A client which is the focused one of its container, but it does not have the
# focus at the moment.
# client.focused_inactive  #000000 #950002 #FFFFFF
# client.focused_inactive #073642 #002b36 $sol_base2 $sol_red

# A client which is not the focused one of its container.
# client.unfocused #073642 #002b36 $sol_base1 $sol_red

#  A client which has its urgency hint activated.
client.urgent $sol_base2 $sol_base3 $sol_red $sol_blue

default_orientation horizontal

# start a terminal
#bindsym $mod+Return exec i3-sensible-terminal
bindsym $mod+Return exec konsole --profile Light
bindsym $mod+F11 exec konsole --profile red
bindsym $mod+F12 exec konsole --profile dark

bindsym $mod+Shift+Return exec konsole --profile dark

# kill focused window
bindsym $mod+Control+x kill

# start dmenu (a program launcher)
bindsym $mod+d exec dmenu_run

# change focus
bindsym $mod+h focus left
bindsym $mod+k focus up
bindsym $mod+l focus right
bindsym $mod+j focus down

# Toggle border between default, none and 1px
bindsym $mod+t border toggle

# alternatively, you can use the cursor keys:
# bindsym $mod+Left focus left
# bindsym $mod+Down focus down
# bindsym $mod+Up focus up
# bindsym $mod+Right focus right

# move focused window
# bindsym $mod+Shift+J move left
# bindsym $mod+Shift+K move down
# bindsym $mod+Shift+L move up
# bindsym $mod+Shift+colon move right

bindsym $mod+Shift+H move left
bindsym $mod+Shift+K move up
bindsym $mod+Shift+L move right
bindsym $mod+Shift+J move down

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+Shift+V split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen
bindsym $mod+Shift+f fullscreen global

# change container layout (stacked, tabbed, default)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout default

# toggle tiling / floating
# bindsym $mod+Shift+space floating toggle
bindsym $mod+Control+F floating toggle
bindsym $mod+Control+S sticky toggle
# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent
bindsym $mod+shift+A focus child

# focus the child container
#bindcode $mod+d focus child

# reload the configuration file
bindsym $mod+Shift+C exec ~/.i3/genconfig.sh; reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+R exec ~/.i3/genconfig.sh; restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+E exit

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym l resize shrink width 1 px or 1 ppt
        bindsym k resize grow height 1 px or 1 ppt
        bindsym j resize shrink height 1 px or 1 ppt
        bindsym h resize grow width 1 px or 1 ppt

        # same bindings, but for the arrow keys
        bindsym 113 resize shrink width 1 px or 1 ppt
        bindsym 116 resize grow height 1 px or 1 ppt
        bindsym 111 resize shrink height 1 px or 1 ppt
        bindsym 114 resize grow width 1 px or 1 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}



bindsym $mod+r mode "resize"

#userid Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
        #font xft:DejaVu Sans 16
        # font xft:Oxygen-Sans Bold 10
        # font xft:Meslo LG S Regular for Powerline Plus Nerd File Types Plus Font Awesome Plus Pomicons 12
        # font pango: Meslo LG S DZ Regular for Powerline Nerd Font Plus Font Awesome Plus Octicons Mono
        font pango: MesloLGMDZ Nerd Font Mono 12
        mode hide
        modifier Mod1
        status_command i3status
        position bottom
        tray_output primary
}

# My own hotkeys
bindsym $mod+F1 exec google-chrome-stable
bindsym $mod+F2 exec dolphin
bindsym $mod+F3 exec ~/bin/cserve
bindsym $mod+F4 exec ~/bin/music
bindsym $mod+F5 exec google-chrome-stable --app='https://youtube.com/tv'
bindsym $mod+F6 exec google-chrome-stable --app='https://app.plex.tv/web/app#'
bindsym $mod+F7 exec google-chrome-stable --app='https://www.netflix.com/browse'
bindsym $mod+F8 workspace $ws2, exec slack
bindsym $mod+Escape exec krunner
# bindsym $ctrl+mod+l exec i3lock
# Jump to urgent window
bindsym $mod+x [urgent=latest] focus

# set $Locker xscreensaver-command -lock && sleep 1

set $mode_system System (l) lock, (e) logout, (s) suspend, (h) hibernate, (r) reboot, (Shift+s) shutdown
mode "$mode_system" {
    bindsym l exec --no-startup-id $Locker, mode "default"
    bindsym e exec --no-startup-id i3-msg exit, mode "default"
    bindsym s exec --no-startup-id $Locker && systemctl suspend, mode "default"
    bindsym h exec --no-startup-id $Locker && systemctl hibernate, mode "default"
    bindsym r exec --no-startup-id systemctl reboot, mode "default"
    bindsym Shift+s exec --no-startup-id systemctl poweroff -i, mode "default"

    # back to normal: Enter or Escape
    bindsym Return mode "default"
    bindsym Escape mode "default"
}

# Enable the menu bar for power managment defined above
bindsym $mod+Pause mode "$mode_system"

# exec_always xmodmap ~/.Xmodmap
exec_always xrdb -merge ~/.Xresources
exec yakuake
exec xflux -z 83702
exec krunner
exec kmix --keepvisibility
